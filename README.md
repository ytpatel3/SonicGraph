# SonicGraph

#### This project implements a graph-driven recommendation engine using Neo4j, where songs are represented as nodes and similarity relationships are computed via cosine similarity on feature vectors. By leveraging graph traversal instead of traditional filtering, the system identifies sonically similar songs that transcend genre boundaries and release eras.
